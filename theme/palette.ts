"use client";

import type { ThemeOptions } from "@mui/material";

const palette: ThemeOptions["palette"] = {
  customPalette: {
    firstColor: "#00ff",
    green: "#ff0",
  },
  inputColor: {
    first: "#eebe00",
    second: "#a8a8a8",
    last: "red",
  },
};

export { palette };
