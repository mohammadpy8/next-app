"use client";

import type { ThemeOptions } from "@mui/material";

const palette: ThemeOptions["palette"] = {
  customPalette: {
    firstColor: "#00ff",
    green: "#ff0",
  },
};

export { palette };
