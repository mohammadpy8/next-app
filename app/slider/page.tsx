import SliderComponent from "@/components/module/sliderComponent/SliderComponent";
import { Box } from "@mui/material";
import React from "react";

const Slider = () => {
  return (
    <Box display={"flex"} justifyContent={"center"}>
      <SliderComponent />
    </Box>
  );
};

export default Slider;
